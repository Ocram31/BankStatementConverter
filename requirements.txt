pdfplumber>=0.11.0
ttkthemes>=3.0
tkinterdnd2>=0.4.0

# Optional: password-protected PDF support (SA banks encrypt statements)
# pip install pikepdf
pikepdf>=9.0.0

# Optional: Excel output with formatting
openpyxl>=3.1.0

# Optional: OCR for FNB fee descriptions rendered as images
# Also requires system package: sudo apt install tesseract-ocr tesseract-ocr-afr
pytesseract>=0.3.10
Pillow>=10.0.0
